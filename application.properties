spring.ai.mcp.server.protocol=STREAMABLE
spring:
    ai:
        mcp:
            client:
                streamable-http:
                    connections:
                        weather-server:
                            url: http://localhost:8080